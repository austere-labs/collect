name = "create_todo_list"
description = "Create a todo list with EXACTLY three items, setting them to 'pending' status and 'medium' priority."
[[parameters]]
name = "item1"
type = "string"
description = "The first todo list item."
[[parameters]]
name = "item2"
type = "string"
description = "The second todo list item."
[[parameters]]
name = "item3"
type = "string"
description = "The third todo list item."
tool_code = """
"""
# This command is designed to create a todo list using a conceptual 'TodoWrite' tool.
# The items are:
# 1. {item1}
# 2. {item2}
# 3. {item3}
# Instructions for the conceptual 'TodoWrite' tool:
# - Use the TodoWrite tool to create these items
# - Set all items to "pending" status
# - Set all items to "medium" priority
# - Do NOT add any additional items
# - Do NOT modify the task descriptions
# Example conceptual call if 'TodoWrite' were available:
# print(default_api.TodoWrite(
#     items=[
]
#         {"description": item1, "status": "pending", "priority": "medium"},
#         {"description": item2, "status": "pending", "priority": "medium"},
#         {"description": item3, "status": "pending", "priority": "medium"}
#     ]
# ))
"""
"""